'use client';
import {useState} from 'react';
import {useRouter} from 'next/navigation';
import {ArrowRight,Eye,EyeOff,ShieldCheck} from 'lucide-react';
export default function Login(){const r=useRouter();const [email,setEmail]=useState('admin@construplata.com');const [password,setPassword]=useState('');const [show,setShow]=useState(false);const [error,setError]=useState('');function submit(e:React.FormEvent){e.preventDefault();if(email.toLowerCase()==='admin@construplata.com'&&password==='Admin123*'){localStorage.setItem('cp-auth','1');r.push('/dashboard')}else setError('Correo o contraseña incorrectos')}return <div className="login-page"><section className="login-showcase"><div className="showcase-brand"><img src="/logo-construplata.jpg" alt="logo"/><span>CONSTRUPLATA</span></div><div className="showcase-copy"><span className="login-kicker">ERP DE CONSTRUCCIÓN</span><h1>Planifica con precisión.<br/><em>Controla cada obra.</em><br/><span>Crece con confianza.</span></h1><p>Cotizaciones, proyectos, bitácoras y finanzas en un solo centro de control.</p><div className="showcase-chips"><span>Proyectos</span><span>Finanzas</span><span>Bitácoras</span></div></div><div className="showcase-foot">Gestión inteligente para construir mejor.</div></section><section className="login-panel"><form className="login-card" onSubmit={submit}><div className="secure-badge"><ShieldCheck size={16}/> Acceso seguro</div><h2>Bienvenido</h2><p>Ingresa a tu centro de control empresarial.</p><label>Correo electrónico<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Contraseña<div className="password-field"><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/><button type="button" className="password-toggle" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<div className="login-error">{error}</div>}<button className="login-button">Entrar al sistema <ArrowRight/></button><small>© 2026 CONSTRUPLATA SRL · República Dominicana</small></form></section></div>}
