'use client';
import Link from 'next/link';
import {usePathname,useRouter} from 'next/navigation';
import {useEffect,useState} from 'react';
import {LayoutDashboard,Users,FileText,FolderKanban,NotebookPen,WalletCards,ReceiptText,CircleDollarSign,HardHat,Landmark,BarChart3,Settings,LogOut,Menu,X,Moon,Sun} from 'lucide-react';
import {DataProvider} from './DataProvider';
const menu=[['/dashboard','Dashboard',LayoutDashboard],['/clientes','Clientes',Users],['/cotizaciones','Cotizaciones',FileText],['/contratos','Contratos',FileText],['/proyectos','Proyectos',FolderKanban],['/bitacoras','Bitácoras',NotebookPen],['/cobros','Cobros y avances',WalletCards],['/facturas','Facturación',ReceiptText],['/gastos','Gastos',CircleDollarSign],['/contratistas','Contratistas',HardHat],['/caja','Caja y bancos',Landmark],['/reportes','Reportes',BarChart3],['/configuracion','Configuración',Settings]] as const;
export default function AppShell({children}:{children:React.ReactNode}){const path=usePathname();const router=useRouter();const [open,setOpen]=useState(false);const [dark,setDark]=useState(false);useEffect(()=>{if(localStorage.getItem('cp-auth')!=='1')router.replace('/login')},[router]);useEffect(()=>{document.documentElement.dataset.theme=dark?'dark':'light'},[dark]);return <DataProvider><div className="shell"><aside className={open?'sidebar open':'sidebar'}><div className="brand"><img src="/logo-construplata.jpg" alt="logo"/><div><b>CONSTRUPLATA</b><span>Control & Ingeniería</span></div><button className="icon mobile" onClick={()=>setOpen(false)}><X/></button></div><nav>{menu.map(([href,label,I])=><Link key={href} href={href} className={path===href?'nav active':'nav'} onClick={()=>setOpen(false)}><I size={18}/><span>{label}</span></Link>)}</nav><div className="side-foot">Versión modular 2.0</div></aside><main className="main"><header className="topbar"><button className="icon mobile" onClick={()=>setOpen(true)}><Menu/></button><div><h1>{menu.find(x=>x[0]===path)?.[1]||'CONSTRUPLATA'}</h1><p>Gestión administrativa y financiera</p></div><div className="top-actions"><button className="icon" onClick={()=>setDark(!dark)}>{dark?<Sun/>:<Moon/>}</button><div className="avatar">JC</div><button className="icon" onClick={()=>{localStorage.removeItem('cp-auth');router.push('/login')}}><LogOut/></button></div></header><div className="content">{children}</div></main></div></DataProvider>}
